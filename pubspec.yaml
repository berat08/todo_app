name: untitled1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  cloud_firestore: ^5.6.7
  google_fonts: ^6.1.0
  flutter_slidable: ^3.0.1
  intl: ^0.19.0
  confetti: ^0.7.0
  table_calendar: ^3.0.9 # YENİ EKLENDİ - Takvim için

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # SDK versiyonuna göre ^3.0.0 veya ^2.0.0 olabilir

flutter:
  uses-material-design: true